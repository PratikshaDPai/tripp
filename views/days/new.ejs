<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add Day</title>
    <link rel="stylesheet" href="/css/styles.css" />
    <script defer src="/js/autocomplete.js"></script>
    <!-- Load JS file -->
  </head>
  <body>
    <div class="container">
      <h1>Add Day</h1>

      <form
        action="/trips/<%= trip._id %>/days"
        method="POST"
        class="create-form"
      >
        <!-- Location Search -->
        <label for="name">Location:</label>
        <input
          type="text"
          id="name"
          name="name"
          placeholder="Search location..."
          autocomplete="off"
          required
        />
        <input type="hidden" name="location" id="locationId" />
        <ul id="autocompleteDropdown" class="dropdown-list"></ul>

        <!-- Activities Dropdown (Populated dynamically) -->
        <label for="activities">Select Activities:</label>
        <select name="activities" id="activities" multiple required>
          <option value="" disabled selected>Select activities</option>
        </select>
        <input type="hidden" name="activity" id="activityId" />
        <ul id="activityDropdown" class="dropdown-list"></ul>

        <label for="date">Date:</label>
        <input type="date" name="date" id="date" required />
        <textarea name="description" id="description" required hidden>
Add Description Here</textarea
        >
        <textarea
          name="notes"
          id="notes"
          rows="3"
          placeholder="Add Notes Here"
        ></textarea>

        <label for="image">Image URL:</label>
        <input type="text" name="image" id="image" />

        <button type="submit" class="add-button">Add Day</button>
      </form>

      <a href="/trips/<%= trip._id %>" class="back-link">← Back to Days</a>
    </div>
  </body>
</html>
